/*
 * Revision counter for LCC_NODE_STANDARD.md, in the same directory.
 * Bump both the int and string macro by one for every row added to the
 * standard's §13 changelog -- the two are meant to stay 1:1.
 *
 * Each project's .snip.software_version composes this with its own patch
 * number: LCC_NODE_STANDARD_REVISION_STR ".<patch>" -- see §7.3.
 */
#ifndef LCC_NODE_STANDARD_VERSION_H
#define LCC_NODE_STANDARD_VERSION_H

#define LCC_NODE_STANDARD_REVISION      14
#define LCC_NODE_STANDARD_REVISION_STR  "14"

#endif
